import pluralize from "pluralize";

const PAGE_TITLE = "center";
const ENDPOINT = pluralize(PAGE_TITLE);
const QUERY_KEY = ENDPOINT;
const MODAL_WIDTH = 800;

export { ENDPOINT, PAGE_TITLE, QUERY_KEY, MODAL_WIDTH };
